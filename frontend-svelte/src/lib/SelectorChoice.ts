
export type ChoiceT = {name: string, value: string};

export let Choice = {
    bestMatch: {name: "Best match", value: "bestMatch"},
    sensitivity: {name: "Sensitivity", value: "sensitivity"},
}

export let Choices = [Choice.bestMatch, Choice.sensitivity]